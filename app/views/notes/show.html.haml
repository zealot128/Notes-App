#note
  #left
    = render @note, :full => true

  #meta
    = render @note.user
    #date
      vor
      =time_ago_in_words @note.created_at
      geschrieben
    #tags
      - @note.tags.each do |tag|
        %span.tag
          =tag.name


